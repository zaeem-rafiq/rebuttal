# Blocker R-15: Outbound SMS Blocked by Carrier (Twilio Error 30034)

## What Was Tried
1. Deployed Rebuttal stack and executed `scripts/reset_demo.py` (`PROOF R-15: reset_demo ok runtime=READY lambdas=3/3 console=200 = PASS`).
2. Executed Rehearsal #1 Scenario S1 ($48 Trail-mix sampler):
   - Confirmed PaymentIntent in Stripe test mode.
   - Deployed Bedrock AgentCore runtime evaluated evidence and auto-submitted to Stripe.
   - Stripe dispute `du_1UCtoFEmho7ai02fOq6d6wNJ` resolved to `status=won`.
   - Verified `no_sms=true` (0 SMS for S1).
3. Executed Scenario S2 ($340 Ceramic pour-over):
   - Bedrock AgentCore hit ApprovalGate ($340 >= $200 threshold).
   - Twilio API accepted outbound message to OWNER_PHONE (+18129551686, SID: `SMbac5925953979e8afe02305a92c6f354`).
   - Inspected Twilio message status via API.

## Exact Error
```json
{
  "sid": "SMbac5925953979e8afe02305a92c6f354",
  "status": "undelivered",
  "error_code": 30034,
  "error_message": null,
  "from": "+18312791727",
  "to": "+18129551686"
}
```
**Twilio Error 30034:** `US A2P 10DLC - Message from an Unregistered Number`.
US mobile network carriers (AT&T, Verizon, T-Mobile) drop outbound Application-to-Person (A2P) messages sent from unregistered 10-digit local codes (10DLC) to US phone numbers.

## Hypothesis
Outbound SMS delivery from Twilio number `+18312791727` to the user's US mobile carrier is blocked downstream by carrier A2P 10DLC compliance filters. All outbound messages in the Twilio account logs to `+18129551686` share this exact carrier rejection. However:
1. Inbound SMS sent from a personal mobile device to `+18312791727` is P2A (Person-to-Application) and is NOT blocked by carrier A2P filters.
2. If inbound SMS is supported, the user can text `2` directly to `+18312791727` to complete the approval gate on the deployed stack. Alternatively, a verified Twilio Toll-Free number or A2P campaign registration is required for carrier delivery.

## Resolution
Option A succeeded: User sent an inbound text `2` directly from their real personal phone (`+18129551686`) to Twilio number `+18312791727` (Twilio SID `SMd91c2c7ba007e7ee0813e2eea6cb4898`).
The `rebuttal-twilio-webhook` Lambda received the webhook, extracted the decision `answer=2`, and invoked the live Bedrock AgentCore runtime (`arn:aws:bedrock-agentcore:us-east-1:292341338711:runtime/rebuttal-pASUe6CVmu`), which executed the concession and updated the dispute to `lost`.
The real phone loop was verified with zero simulated replies.

