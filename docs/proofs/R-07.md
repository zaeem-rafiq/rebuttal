# Proof of Success — R-07: AgentCore Memory: session persistence + long-term outcomes

**Date:** 2026-09-05  
**Issue:** HAC-9 (R-07)  
**Agent ARN:** `arn:aws:bedrock-agentcore:us-east-1:292341338711:runtime/rebuttal-pASUe6CVmu`  
**Endpoint:** `DEFAULT (READY)`  
**Memory ID:** `rebuttal_mem-Idf0xfCGuL`  
**Session ID:** `rebuttal-dp_S2-843e9bf4236345f28e9a0a837080fbc7`  
**Log Group:** `/aws/bedrock-agentcore/runtimes/rebuttal-pASUe6CVmu-DEFAULT`  

---

### Proof Lines

PROOF R-07: cloud interrupt persisted — invoke S2 (stop_reason=interrupt) then invoke approval in a separate call → dispute closed = PASS
PROOF R-07: memory retrieve 'product_not_received' returns >=1 outcome record = PASS

---

### Verification Summary

1. **Cloud Interrupt Persisted Across Separate Invocations:**
   - Call 1: Invoked runtime with `{"type": "dispute.created", "dispute_id": "dp_S2", "scenario": "S2"}` on session `rebuttal-dp_S2-843e9bf4236345f28e9a0a837080fbc7`.
     - Multi-agent evidence pipeline completed, evaluating high-value repeat customer Jessica Lee.
     - Human gate triggered: `Twilio SMS approval prompt` sent to owner, executor paused with `stop_reason=interrupt`, gate status recorded in AgentCore Memory session state.
     - CloudWatch verified: `Executor finished: stop_reason=interrupt, gate_status=required`.
   - Call 2: Invoked approval in a completely separate runtime invocation call on the same session ID `rebuttal-dp_S2-843e9bf4236345f28e9a0a837080fbc7` with `{"type": "approval", "dispute_id": "dp_S2", "answer": "1"}`.
     - Successfully resumed state from AgentCore memory, processed approval concession, updated dispute status, and emitted:
       - Payload response: `{"accepted": true, "type": "approval", "dispute_id": "dp_S2", "status": "closed", "final_status": "lost"}`
       - CloudWatch verified: `approval processed for dp_S2: dispute closed status=lost` and `dispute closed status=lost`.

2. **Long-Term Memory Retrieval:**
   - Initialized semantic strategy `merchant_outcomes` in Bedrock AgentCore Memory resource `rebuttal_mem-Idf0xfCGuL` under namespace `/merchant/{actorId}/outcomes`.
   - Seeded and queried dispute outcomes via `retrieve_past_outcomes("product_not_received")`.
   - Retrieval query returned 4 records from Bedrock AgentCore memory, confirming >= 1 outcome record retrieved and integrated into merchant history context.

3. **ADR-001 Recorded:**
   - Architecture Decision Record created at `docs/decisions/ADR-001.md` documenting Bedrock AgentCore Memory architecture, short-term session storage, long-term semantic outcome indexing, and fallback layers.

4. **Automated Test Suite:**
   - 51/51 tests passing in `pytest` (including `tests/test_memory.py`).
